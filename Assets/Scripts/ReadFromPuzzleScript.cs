using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReadFromPuzzleScript : MonoBehaviour {

    // Lấy các object từ puzzleScript ra và tách lấy các object
	void GetObjects()
    {
        // sau khi lấy xòn nó sẽ trả lại vào một bảng gồm có tên, màu sử dụng và danh sách các điểm màu
    }
}
